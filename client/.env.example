# Client environment (build-time, Vite).
# Leave EMPTY when serving behind the Nginx reverse proxy (same-origin).
# Set to the API origin (e.g. https://api.example.com) for split-origin deploys.
VITE_BACKEND_URL=
